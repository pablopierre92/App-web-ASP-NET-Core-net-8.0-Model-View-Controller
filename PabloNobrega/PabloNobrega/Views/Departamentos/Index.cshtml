@model IEnumerable<Departamento>

@{
    ViewData["Title"] = "Departamentos";
}


<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-5">
        <h1>@ViewData["Title"]</h1>
        <a class="btn btn-success mx-2" asp-asp-controller="Departamentos" asp-action="Cadastrar"><i class="bi bi-file-earmark-plus mx-1"></i>Cadastrar novo departamento</a>
    </div>

    @if (TempData["MensagemSucesso"] != null)
    {
        <div class="alert alert-success text-dark" role="alert">
            @TempData["MensagemSucesso"]
        </div>
    }

    @if (TempData["MensagemErro"] != null)
    {
        <div class="alert alert-danger text-dark" role="alert">
            @TempData["MensagemErro"]
        </div>
    }


    <table id="Emprestimos" class="table table-striped table-hover">
        <thead>
            <tr>
                <th scope="col">Nome</th>
                
                <th></th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null)
            {

                @foreach (var departamento in Model)
                {
                    <tr>
                        
                        <td>@departamento.Nome</td>
                        <td>
                            <a asp-controller="Departamentos" asp-action="Editar" asp-route-id="@departamento.Id" class="btn btn-warning mx-2"><i class="bi bi-pencil-square mx-1"></i>Editar</a>
                            <a asp-controller="Departamentos" asp-action="Excluir" asp-route-id="@departamento.Id" class="btn btn-danger mx-2"><i class="bi bi-trash3-fill mx-1"></i>Excluir</a>
                        </td>
                    </tr>
                }

            }
        </tbody>
    </table>

</div>